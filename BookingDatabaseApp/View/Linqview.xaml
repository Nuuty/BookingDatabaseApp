<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookingDatabaseApp.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:BookingDatabaseApp.ViewModel"
    x:Class="BookingDatabaseApp.View.Linqview"
    mc:Ignorable="d">

    <Page.DataContext>
        <ViewModel:ViewModel/>
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView x:Name="listView" HorizontalAlignment="Left" Height="329" Margin="0,-4,0,0" VerticalAlignment="Top" Width="395" ItemsSource="{Binding Catalog.Hoteller, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button x:Name="button" Content="Roskilde" HorizontalAlignment="Left" Margin="25,353,0,0" VerticalAlignment="Top" Command="{Binding HotelsinRoskildeCommand, Mode=OneWay}"/>
        <ListView x:Name="listView1" HorizontalAlignment="Left" Height="249" Margin="475,11,0,0" VerticalAlignment="Top" Width="155" ItemsSource="{Binding HotelHandler.AllHotelAndRooms, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <Button x:Name="button1" Content="Rooms" HorizontalAlignment="Left" Margin="135,353,0,0" VerticalAlignment="Top" Command="{Binding HotelsandRoomsCommand, Mode=OneWay}"/>
    </Grid>
</Page>
